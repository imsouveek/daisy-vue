import { Canvas, Meta, Controls, Story } from '@storybook/addon-docs/blocks'
import * as PrimaryStory from './Playground.stories'
import * as VariationsStories from './Variations.stories'

;<Meta title="Components/vMask/Docs" />

# vMask

<Meta of={PrimaryStory} />

A mask directive to crop edges of images to common shapes. It
uses the [Daisy UI Mask component](https://daisyui.com/components/mask/) and exposes most of the
available options.

<Canvas of={PrimaryStory.Playground} />

<Controls />

## Usage

The mask can be modified using type and modifier

### Variations

<div className="p-8 border-1 rounded border-gray-200 shadow-sm">
    <Story of={VariationsStories.AllMasks} />
</div>
